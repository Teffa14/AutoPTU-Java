name: Chronicler Profile Match Contract

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  contract:
    runs-on: ubuntu-24.04
    steps:
      - name: Check out Java port
        uses: actions/checkout@v4

      - name: Check out pinned Python oracle
        uses: actions/checkout@v4
        with:
          repository: Teffa14/AutoPTU
          ref: 16d228efa63aabecb67fa788959a359aac7f8f03
          path: python-oracle

      - name: Inspect pinned Chronicler profile matching
        run: |
          python3 -m py_compile tools/python/export_chronicler_profile_match_contract.py
          python3 tools/python/export_chronicler_profile_match_contract.py \
            --source-root python-oracle \
            --output build/oracle/chronicler-profile-match.txt
          cat build/oracle/chronicler-profile-match.txt
