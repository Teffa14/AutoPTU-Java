name: Runtime Reaction Ownership Parity

on:
  pull_request:
    paths:
      - "src/main/java/io/autoptu/core/runtime/ReactionOwnershipSource.java"
      - "src/main/java/io/autoptu/core/runtime/MoveReactionOwnershipSource.java"
      - "src/main/java/io/autoptu/core/runtime/RuntimeReactionOwnershipResolver.java"
      - "src/main/java/io/autoptu/core/runtime/BattleRuntimeState.java"
      - "src/test/java/io/autoptu/core/runtime/RuntimeReactionOwnershipResolverTest.java"
      - "src/test/java/io/autoptu/core/runtime/RuntimeReactionOwnershipOracleParityTest.java"
      - "tools/python/export_attack_of_opportunity_ownership_fixture.py"
      - ".github/workflows/runtime-reaction-ownership-parity.yml"
  push:
    branches:
      - main

jobs:
  parity:
    runs-on: ubuntu-24.04
    steps:
      - name: Check out Java port
        uses: actions/checkout@v4

      - name: Check out pinned Python oracle
        uses: actions/checkout@v4
        with:
          repository: Teffa14/AutoPTU
          ref: 16d228efa63aabecb67fa788959a359aac7f8f03
          path: python-oracle

      - name: Use preinstalled Java 21 and Gradle
        run: |
          echo "JAVA_HOME=$JAVA_HOME_21_X64" >> "$GITHUB_ENV"
          echo "$JAVA_HOME_21_X64/bin" >> "$GITHUB_PATH"
          "$JAVA_HOME_21_X64/bin/java" -version
          gradle --version

      - name: Export pinned Python ownership fixture
        run: |
          python3 -m py_compile tools/python/export_attack_of_opportunity_ownership_fixture.py
          python3 tools/python/export_attack_of_opportunity_ownership_fixture.py \
            --source-root python-oracle \
            --output build/oracle/reaction-ownership.tsv

      - name: Run runtime reaction ownership parity
        env:
          AUTOPTU_REACTION_OWNERSHIP_ORACLE: ${{ github.workspace }}/build/oracle/reaction-ownership.tsv
        run: |
          gradle test \
            --tests io.autoptu.core.runtime.RuntimeReactionOwnershipResolverTest \
            --tests io.autoptu.core.runtime.RuntimeReactionOwnershipOracleParityTest \
            --stacktrace
